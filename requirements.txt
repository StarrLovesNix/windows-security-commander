# Security Commander for Windows — Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Python 3.10+ required.

# Core — real-time process and connection monitoring
psutil>=5.9.0

# GUI — desktop application
PyQt6>=6.4.0

# Terminal dashboard (kept for --no-ui / headless mode)
rich>=13.0.0

# Windows Event Log, WMI, and Win32 APIs
# After pip install, run: python -m pywin32_postinstall -install
pywin32>=306

# Threat intelligence IP reputation lookups
requests>=2.28.0

# Windows desktop toast notifications (fallback outside PyQt6)
plyer>=2.1.0
